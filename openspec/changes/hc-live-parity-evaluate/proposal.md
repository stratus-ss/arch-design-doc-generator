# Change Proposal: hc-live-parity-evaluate

> **STATUS: ARCHIVED**
> Merged into `openspec/specs/hc-report-engine/spec.md` on 2026-08-28.
> Plan: `cursor_plans/hc_tsr_live_parity_evaluate_2026-08-28.md`

Baseline: `openspec/specs/hc-report-engine/spec.md`. Do not modify `hc-feedback-chunk-h` or `hc-live-parity-collect`.

Without TSR HTML, the **core** profile still emits family-level native checks that reparse Plan 2a JSON (and already-collected APIs), with TSR-matched PASS/FAIL/WARNING/INFO/NOT_APPLICABLE/SKIPPED. Full consultant-voice KB narratives and `content_from` aliases are out of this change.

## Why

Plan 2a wrote optional CR JSON. Evaluators still SKIPPED or scored the wrong object. Native Chapter 7 must work on **core** without `--check-profile advisory`.
